/**
 * Default number of items to display per page in paginated lists.
 */
export const DEFAULT_PAGINATION_PAGE_SIZE = 15;

/**
 * Number of items per page for demo pagination components.
 */
export const DEMO_PAGINATION_PAGE_SIZE = 3;

/**
 * Maximum number of options allowed in a select menu.
 * Discord API limit is 25.
 */
export const MAX_SELECT_MENU_OPTIONS = 25;

/**
 * Maximum number of buttons allowed in a single action row.
 * Discord API limit is 5.
 */
export const MAX_BUTTONS_PER_ROW = 5;
